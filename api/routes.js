const express = require('express');
const calculadoraController = require('./src/controller/CalculadoraController');
const router = express.Router();

router.post("/soma", calculadoraController. );
module.exports = router;